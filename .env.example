# 腾讯云API配置
# 请将下面的值替换为你的实际API密钥
# 从腾讯云控制台获取: https://console.cloud.tencent.com/cam/capi

TENCENTCLOUD_SECRET_ID=你的SecretId
TENCENTCLOUD_SECRET_KEY=你的SecretKey

# Excel文件配置
# 指定要写入的Excel文件完整路径
EXCEL_FILE_PATH=D:\工作文件\OCR识别结果.xlsx
# Excel文件名（用于xlwings识别，从完整路径中提取）
EXCEL_FILE_NAME=OCR识别结果.xlsx

# 指定要写入的工作表名称（可选，不填则使用第一个工作表）
EXCEL_SHEET_NAME=识别结果

# 排序后的新工作表名称
EXCEL_SORTED_SHEET_NAME=排序结果

# OCR过滤配置
# 无效物品名的正则表达式模式（用逗号分隔）
# 这些模式用于过滤掉纯数字、符号等无效的物品名
FILTER_INVALID_ITEM_PATTERNS=^\\+\\d+$,^\\*\\d+$,^-\\d+$,^\\d+$,^[\\+\\-\\*]\\d+

# 无效物品名的文本列表（用逗号分隔）
# 这些特定文本会被过滤掉，不作为物品名
FILTER_INVALID_ITEM_TEXTS=关闭,定,图片,附加,附加最大值

# 标题黑名单（用逗号分隔）
# 这些文本不会被识别为标题
FILTER_TITLE_BLACKLIST=附加最大值,物品名,图片,关闭

# 类别排序配置
# 类别优先级顺序（用逗号分隔，不在此列表中的类别会按内容数量排序）
# 可以根据需要调整顺序，比如按照节日时间顺序排列
CATEGORY_PRIORITY_ORDER=春,情人,元宵,劳动,端午,6月心语限时,七夕,中秋,国庆,万圣,圣诞,寒假,冬季,游乐场,魔法装饰,下10层,充值

# 类别名称清理配置
# 需要从类别名中移除的修饰词（用逗号分隔）
# 这些词会被从类别名中移除，以便相似的类别能够合并
# 例如："万圣节精品套装" 和 "万圣节套装" 都会被归类为 "万圣节"
CATEGORY_MODIFIERS_TO_REMOVE=精品,动态,年,叮当猫,节
